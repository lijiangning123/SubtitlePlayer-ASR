# License Files

This directory stores component-level license texts.

Required before publishing a release artifact:

- Add full GPL-3.0 text as `GPL-3.0.txt`.
- Keep `MIT-SubtitlePlayer.txt` for the original frontend.
- Add license texts for bundled Python, FFmpeg, AsrTools, and bundled Python
  packages if those files are included in the release artifact.
